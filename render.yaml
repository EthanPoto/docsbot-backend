services:
  - type: web
    name: docsbot-backend
    env: node
    plan: free
    region: ohio
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
    healthCheckPath: /healthz
    autoDeploy: true
